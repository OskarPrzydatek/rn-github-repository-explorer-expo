import { StyleSheet } from 'react-native';

export const collapseStyles = StyleSheet.create({
  collapse: {
    overflow: 'hidden',
  },
  contentWrapper: {
    width: '100%',
    position: 'absolute',
  },
});
